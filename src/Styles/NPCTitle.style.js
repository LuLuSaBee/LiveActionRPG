import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 0.08,
    alignItems: 'center',
    justifyContent: 'center',
  },
  text: {
    fontSize: 30,
    fontWeight: '900',
  },
});
